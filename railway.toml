# Hyvision One Root Directory - Railway 설정
# 주의: 이 디렉토리는 배포용이 아닙니다.
# 각 서비스는 hvo-server와 hvo-frontend 디렉토리에서 별도로 배포됩니다.

[build]
builder = "nixpacks"

[deploy]
# 루트 디렉토리는 배포하지 않음
# Railway 서비스 설정에서 Root Directory를 지정해야 합니다.
# - 백엔드: hvo-server
# - 프론트엔드: hvo-frontend
startCommand = "echo 'Monorepo root detected. Set Root Directory to hvo-server or hvo-frontend in Railway service settings.' && exit 1"
